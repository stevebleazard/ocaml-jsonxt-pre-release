(jbuild_version 1)

(ocamllex (lexxer))

(menhir
 ((flags (--only-tokens))
   (modules (tokens))))

(menhir
 ((merge_into parser)
  (flags (--external-tokens Tokens))
   (modules (tokens parser))))

(executable
 ((name lexxer)
  (libraries (core core_bench))))

;(alias
; ((name DEFAULT)
;  (deps (lexxer.exe))))
